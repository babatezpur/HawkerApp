<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HawkerApp"
        tools:targetApi="34">
        <activity
            android:name="com.hawkerapp.app.views.PreHawkerScreenActivity"
            android:exported="false" />
        <activity
            android:name="com.hawkerapp.app.views.HawkerFormActivity"
            android:exported="false" />
        <activity
            android:name="com.hawkerapp.app.views.PreUserScreenSplashActivity"
            android:exported="false" />
        <activity
            android:name="com.hawkerapp.app.views.UserViewActivity"
            android:exported="false" />
        <activity
            android:name="com.hawkerapp.app.views.HawkerViewActivity"
            android:exported="false" />
        <activity
            android:name="com.hawkerapp.app.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
    </application>

</manifest>